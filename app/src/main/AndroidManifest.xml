<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ch.heigvd.sym_labo2">

    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SYMLabo2">
        <activity
            android:name=".GraphqlActivity"
            android:exported="true" />
        <activity
            android:name=".CompressActivity"
            android:exported="true" />
        <activity
            android:name=".SerializeActivity"
            android:exported="true" />
        <activity
            android:name=".DifferActivity"
            android:exported="true" />
        <activity
            android:name=".AsyncActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>